<UserControl x:Class="TemplateWpfBlankApp.App.Views.Pages.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <Border Style="{StaticResource CardBorderStyle}">
                    <StackPanel>
                        <TextBlock FontSize="20" FontWeight="SemiBold" Text="Template focus" />
                        <TextBlock Margin="0,10,0,0" TextWrapping="Wrap"
                                   Text="Use this shell as the starting point for apps that read or update data from SQL, SharePoint, Microsoft Graph, and Power BI with company SSO-aware connectors." />
                        <TextBlock Margin="0,14,0,0" FontWeight="SemiBold" Text="Included common capabilities" />
                        <ItemsControl Margin="0,8,0,0">
                            <ItemsControl.Items>
                                <sys:String xmlns:sys="clr-namespace:System;assembly=mscorlib">Reusable shell navigation</sys:String>
                                <sys:String xmlns:sys="clr-namespace:System;assembly=mscorlib">Shared data table and detail view</sys:String>
                                <sys:String xmlns:sys="clr-namespace:System;assembly=mscorlib">Editable form with save/reset pattern</sys:String>
                                <sys:String xmlns:sys="clr-namespace:System;assembly=mscorlib">Connection test dashboard</sys:String>
                                <sys:String xmlns:sys="clr-namespace:System;assembly=mscorlib">Settings and profile persistence</sys:String>
                                <sys:String xmlns:sys="clr-namespace:System;assembly=mscorlib">Activity log export</sys:String>
                            </ItemsControl.Items>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Margin="0,4,0,0" Text="•  {Binding}" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardBorderStyle}">
                    <StackPanel>
                        <TextBlock FontSize="20" FontWeight="SemiBold" Text="Recent activity" />
                        <ListBox Margin="0,12,0,0"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 ItemsSource="{Binding RecentActivities}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{StaticResource PanelBorderBrush}" BorderThickness="0,0,0,1" Padding="0,10">
                                        <StackPanel>
                                            <TextBlock FontWeight="SemiBold" Text="{Binding Message}" />
                                            <TextBlock Foreground="{StaticResource SubtleTextBrush}" Text="{Binding Category}" />
                                            <TextBlock FontSize="12"
                                                       Foreground="{StaticResource SubtleTextBrush}"
                                                       Text="{Binding Timestamp, StringFormat='{}{0:yyyy-MM-dd HH:mm:ss}'}" />
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </Border>
            </StackPanel>

            <StackPanel Grid.Column="2">
                <Border Style="{StaticResource CardBorderStyle}">
                    <StackPanel>
                        <TextBlock FontSize="20" FontWeight="SemiBold" Text="Active profile" />
                        <TextBlock Margin="0,12,0,0" Text="{Binding Profile.ProfileName}" />
                        <TextBlock Foreground="{StaticResource SubtleTextBrush}" Text="{Binding Profile.EnvironmentName}" />
                        <TextBlock Margin="0,12,0,0" FontWeight="SemiBold" Text="User alias" />
                        <TextBlock Text="{Binding Profile.UserAlias}" />
                        <TextBlock Margin="0,12,0,0" FontWeight="SemiBold" Text="Visible columns" />
                        <TextBlock TextWrapping="Wrap" Text="{Binding Profile.VisibleColumns}" />
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardBorderStyle}">
                    <StackPanel>
                        <TextBlock FontSize="20" FontWeight="SemiBold" Text="Connections snapshot" />
                        <ItemsControl Margin="0,12,0,0" ItemsSource="{Binding Connections}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{StaticResource PanelBorderBrush}" BorderThickness="0,0,0,1" Padding="0,10">
                                        <StackPanel>
                                            <TextBlock FontWeight="SemiBold" Text="{Binding Name}" />
                                            <TextBlock Foreground="{StaticResource SubtleTextBrush}" Text="{Binding SystemType}" />
                                            <TextBlock TextWrapping="Wrap" Text="{Binding Endpoint}" />
                                            <TextBlock FontSize="12" Foreground="{StaticResource SubtleTextBrush}" Text="{Binding Status}" />
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
