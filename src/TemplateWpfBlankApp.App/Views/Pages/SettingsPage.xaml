<UserControl x:Class="TemplateWpfBlankApp.App.Views.Pages.SettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <Border Style="{StaticResource CardBorderStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="220" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="12" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="12" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="12" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="12" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="12" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="12" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="12" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock FontWeight="SemiBold" Text="Profile name" />
                    <TextBox Grid.Column="1" Text="{Binding Profile.ProfileName, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Grid.Row="2" FontWeight="SemiBold" Text="Environment" />
                    <ComboBox Grid.Row="2" Grid.Column="1" ItemsSource="{Binding EnvironmentOptions}" SelectedItem="{Binding Profile.EnvironmentName}" />

                    <TextBlock Grid.Row="4" FontWeight="SemiBold" Text="Default page" />
                    <ComboBox Grid.Row="4" Grid.Column="1" ItemsSource="{Binding DefaultPageOptions}" SelectedItem="{Binding Profile.DefaultPage}" />

                    <TextBlock Grid.Row="6" FontWeight="SemiBold" Text="Default status filter" />
                    <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding Profile.DefaultStatusFilter, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Grid.Row="8" FontWeight="SemiBold" Text="Default owner filter" />
                    <TextBox Grid.Row="8" Grid.Column="1" Text="{Binding Profile.DefaultOwnerFilter, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Grid.Row="10" FontWeight="SemiBold" Text="Request timeout (sec)" />
                    <TextBox Grid.Row="10" Grid.Column="1" Text="{Binding Profile.RequestTimeoutSeconds, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Grid.Row="12" FontWeight="SemiBold" Text="Log level" />
                    <ComboBox Grid.Row="12" Grid.Column="1" ItemsSource="{Binding LogLevelOptions}" SelectedItem="{Binding Profile.LogLevel}" />

                    <StackPanel Grid.Row="14" Grid.ColumnSpan="2">
                        <TextBlock FontWeight="SemiBold" Text="Visible columns" />
                        <TextBox Margin="0,8,0,0" Text="{Binding Profile.VisibleColumns, UpdateSourceTrigger=PropertyChanged}" />
                        <CheckBox Margin="0,12,0,0" Content="Default to read-only mode" IsChecked="{Binding Profile.UseReadOnlyMode}" />
                        <CheckBox Margin="0,8,0,0" Content="Show advanced navigation pages" IsChecked="{Binding Profile.ShowAdvancedPages}" />
                        <CheckBox Margin="0,8,0,0" Content="Administrator mode" IsChecked="{Binding Profile.IsAdministrator}" />
                    </StackPanel>
                </Grid>
            </Border>

            <Border Style="{StaticResource CardBorderStyle}">
                <DockPanel>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                        <Button Margin="0,0,8,0" Padding="14,8" Command="{Binding LoadCurrentCommand}" Content="Load Current" />
                        <Button Margin="0,0,8,0" Padding="14,8" Command="{Binding SaveCurrentCommand}" Content="Save Current" />
                        <Button Padding="14,8" Command="{Binding ResetTemplateCommand}" Content="Reset Template" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock FontWeight="SemiBold" Text="Storage" />
                        <TextBlock Foreground="{StaticResource SubtleTextBrush}" Text="{Binding StorageFolderPath}" TextWrapping="Wrap" />
                    </StackPanel>
                </DockPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
