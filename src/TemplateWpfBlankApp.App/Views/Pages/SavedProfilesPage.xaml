<UserControl x:Class="TemplateWpfBlankApp.App.Views.Pages.SavedProfilesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource CardBorderStyle}" Margin="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding NewProfileName, UpdateSourceTrigger=PropertyChanged}" />
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Margin="0,0,8,0" Padding="14,8" Command="{Binding RefreshCommand}" Content="Refresh" />
                    <Button Margin="0,0,8,0" Padding="14,8" Command="{Binding SaveAsCommand}" Content="Save As" />
                    <Button Margin="0,0,8,0" Padding="14,8" Command="{Binding LoadCommand}" Content="Load Selected" />
                    <Button Padding="14,8" Command="{Binding DeleteCommand}" Content="Delete Selected" />
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}" Margin="0">
            <ListBox ItemsSource="{Binding Profiles}" SelectedItem="{Binding SelectedProfileName, Mode=TwoWay}" />
        </Border>
    </Grid>
</UserControl>
