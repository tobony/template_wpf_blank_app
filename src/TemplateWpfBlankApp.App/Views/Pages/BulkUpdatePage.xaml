<UserControl x:Class="TemplateWpfBlankApp.App.Views.Pages.BulkUpdatePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource CardBorderStyle}" Margin="0">
            <DockPanel>
                <Button DockPanel.Dock="Right" Padding="14,8" Command="{Binding GeneratePreviewCommand}" Content="Generate Preview" />
                <StackPanel>
                    <TextBlock FontWeight="SemiBold" Text="Preview-first bulk update pipeline" />
                    <TextBlock Foreground="{StaticResource SubtleTextBrush}" Text="Template for CSV/Excel driven batch operations with validation before commit." />
                </StackPanel>
            </DockPanel>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}" Margin="0">
            <DataGrid ItemsSource="{Binding PreviewRows}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="#" Binding="{Binding RowNumber}" Width="60" />
                    <DataGridTextColumn Header="Item" Binding="{Binding ItemTitle}" Width="2*" />
                    <DataGridTextColumn Header="Action" Binding="{Binding ProposedAction}" Width="2*" />
                    <DataGridTextColumn Header="Validation" Binding="{Binding ValidationStatus}" Width="*" />
                    <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="2*" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
