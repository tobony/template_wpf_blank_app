<UserControl x:Class="TemplateWpfBlankApp.App.Views.Pages.DataViewPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource CardBorderStyle}" Margin="0">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <Button Padding="14,8" Command="{Binding RefreshCommand}" Content="Refresh" />
                </StackPanel>
                <StackPanel>
                    <TextBlock FontWeight="SemiBold" Text="Shared table view" />
                    <TextBlock Foreground="{StaticResource SubtleTextBrush}" Text="Select a row to reuse it in the Edit page." />
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}" Margin="0">
                <DataGrid ItemsSource="{Binding Items}"
                          SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="90" />
                        <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="2*" />
                        <DataGridTextColumn Header="Source" Binding="{Binding SourceSystem}" Width="*" />
                        <DataGridTextColumn Header="Owner" Binding="{Binding Owner}" Width="*" />
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*" />
                        <DataGridTextColumn Header="Updated" Binding="{Binding LastUpdated, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Width="160" />
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}" Margin="0">
                <StackPanel>
                    <TextBlock FontSize="18" FontWeight="SemiBold" Text="Selected item" />
                    <TextBlock Margin="0,12,0,0" FontWeight="SemiBold" Text="{Binding SelectedItem.Title, TargetNullValue=Select a row}" />
                    <TextBlock Foreground="{StaticResource SubtleTextBrush}" Text="{Binding SelectedItem.SourceSystem}" />
                    <TextBlock Margin="0,12,0,0" TextWrapping="Wrap" Text="{Binding SelectedItem.Notes}" />
                    <TextBlock Margin="0,12,0,0" FontWeight="SemiBold" Text="Owner" />
                    <TextBlock Text="{Binding SelectedItem.Owner}" />
                    <TextBlock Margin="0,12,0,0" FontWeight="SemiBold" Text="Status" />
                    <TextBlock Text="{Binding SelectedItem.Status}" />
                    <TextBlock Margin="0,12,0,0" FontWeight="SemiBold" Text="Read-only" />
                    <TextBlock Text="{Binding SelectedItem.IsReadOnly}" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
