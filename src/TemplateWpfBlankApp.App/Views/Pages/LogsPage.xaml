<UserControl x:Class="TemplateWpfBlankApp.App.Views.Pages.LogsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource CardBorderStyle}" Margin="0">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <ComboBox Width="150" Margin="0,0,8,0" ItemsSource="{Binding SeverityOptions}" SelectedItem="{Binding SelectedSeverity}" />
                    <Button Margin="0,0,8,0" Padding="14,8" Command="{Binding ExportCommand}" Content="Export" />
                    <Button Padding="14,8" Command="{Binding ClearCommand}" Content="Clear" />
                </StackPanel>
                <StackPanel>
                    <TextBlock FontWeight="SemiBold" Text="Activity log" />
                    <TextBlock Foreground="{StaticResource SubtleTextBrush}"
                               Text="{Binding IsAdministrator, StringFormat=Administrator-only clear enabled: {0}}" />
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}" Margin="0">
                <DataGrid ItemsSource="{Binding FilteredEntries}"
                          SelectedItem="{Binding SelectedEntry, Mode=TwoWay}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp, StringFormat='{}{0:yyyy-MM-dd HH:mm:ss}'}" Width="180" />
                        <DataGridTextColumn Header="Severity" Binding="{Binding Severity}" Width="100" />
                        <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="120" />
                        <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="2*" />
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}" Margin="0">
                <StackPanel>
                    <TextBlock FontSize="18" FontWeight="SemiBold" Text="Selected log detail" />
                    <TextBlock Margin="0,12,0,0" FontWeight="SemiBold" Text="{Binding SelectedEntry.Message, TargetNullValue=Select a log entry}" TextWrapping="Wrap" />
                    <TextBlock Margin="0,8,0,0" Foreground="{StaticResource SubtleTextBrush}" Text="{Binding SelectedEntry.Category}" />
                    <TextBlock Margin="0,8,0,0" Text="{Binding SelectedEntry.Severity}" />
                    <TextBlock Margin="0,12,0,0" FontWeight="SemiBold" Text="Detail / exception" />
                    <TextBlock TextWrapping="Wrap" Text="{Binding SelectedEntry.Detail}" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
