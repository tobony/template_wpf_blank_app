<UserControl x:Class="TemplateWpfBlankApp.App.Views.Pages.LogsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource CardBorderStyle}" Margin="0">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <Button Margin="0,0,8,0" Padding="14,8" Command="{Binding ExportCommand}" Content="Export" />
                    <Button Padding="14,8" Command="{Binding ClearCommand}" Content="Clear" />
                </StackPanel>
                <StackPanel>
                    <TextBlock FontWeight="SemiBold" Text="Activity log" />
                    <TextBlock Foreground="{StaticResource SubtleTextBrush}" Text="Keep audit-friendly details for refresh, edit, and connection workflows." />
                </StackPanel>
            </DockPanel>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}" Margin="0">
            <DataGrid ItemsSource="{Binding Entries}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp, StringFormat='{}{0:yyyy-MM-dd HH:mm:ss}'}" Width="180" />
                    <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="120" />
                    <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="2*" />
                    <DataGridTextColumn Header="Detail" Binding="{Binding Detail}" Width="2*" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
