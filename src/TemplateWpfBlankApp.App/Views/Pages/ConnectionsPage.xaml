<UserControl x:Class="TemplateWpfBlankApp.App.Views.Pages.ConnectionsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource CardBorderStyle}" Margin="0">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <Button Margin="0,0,8,0" Padding="14,8" Command="{Binding TestSelectedCommand}" Content="Test Selected" />
                    <Button Margin="0,0,8,0" Padding="14,8" Command="{Binding TestAllCommand}" Content="Test All" />
                    <Button Padding="14,8" Command="{Binding SaveConnectionSettingsCommand}" Content="Save Parameters" />
                </StackPanel>
                <StackPanel>
                    <TextBlock FontWeight="SemiBold" Text="Connection manager" />
                    <TextBlock Foreground="{StaticResource SubtleTextBrush}"
                               Text="{Binding IsAdministrator, StringFormat=Administrator-managed updates enabled: {0}}" />
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}" Margin="0">
                <DataGrid ItemsSource="{Binding Connections}"
                          SelectedItem="{Binding SelectedConnection, Mode=TwoWay}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="*" />
                        <DataGridTextColumn Header="Type" Binding="{Binding SystemType, UpdateSourceTrigger=PropertyChanged}" Width="*" />
                        <DataGridTextColumn Header="Endpoint" Binding="{Binding Endpoint, UpdateSourceTrigger=PropertyChanged}" Width="2*" />
                        <DataGridTextColumn Header="Auth" Binding="{Binding Authentication, UpdateSourceTrigger=PropertyChanged}" Width="*" />
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120" />
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}" Margin="0">
                <StackPanel>
                    <TextBlock FontSize="18" FontWeight="SemiBold" Text="Selected connection detail" />
                    <TextBlock Margin="0,12,0,0" FontWeight="SemiBold" Text="{Binding SelectedConnection.Name, TargetNullValue=Select a connection}" />
                    <TextBlock Foreground="{StaticResource SubtleTextBrush}" Text="{Binding SelectedConnection.SystemType}" />
                    <TextBlock Margin="0,12,0,0" TextWrapping="Wrap" Text="{Binding SelectedConnection.Endpoint}" />
                    <TextBlock Margin="0,12,0,0" FontWeight="SemiBold" Text="Authentication status" />
                    <TextBlock Text="{Binding SelectedConnection.AuthenticationStatus}" />
                    <TextBlock Margin="0,12,0,0" FontWeight="SemiBold" Text="Token / session expiry" />
                    <TextBlock Text="{Binding SelectedConnection.AuthenticationExpiresAt, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" />
                    <TextBlock Margin="0,12,0,0" FontWeight="SemiBold" Text="Admin managed" />
                    <TextBlock Text="{Binding SelectedConnection.IsAdminManaged}" />
                    <TextBlock Margin="0,12,0,0" FontWeight="SemiBold" Text="Notes" />
                    <TextBlock TextWrapping="Wrap" Text="{Binding SelectedConnection.Notes}" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
