<Window x:Class="WpfBlankApp.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}"
        Height="820"
        Width="1400"
        MinWidth="1100"
        MinHeight="700"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <ControlTemplate x:Key="TopNavigationMenuItemTemplate" TargetType="MenuItem">
            <Grid>
                <Border x:Name="TopItemBorder"
                        Padding="14,12"
                        Background="Transparent"
                        CornerRadius="10">
                    <DockPanel>
                        <TextBlock Foreground="White"
                                   FontSize="15"
                                   FontWeight="SemiBold"
                                   Text="{TemplateBinding Header}" />
                    </DockPanel>
                </Border>
                <Popup IsOpen="{TemplateBinding IsSubmenuOpen}"
                       AllowsTransparency="True"
                       Focusable="False"
                       Placement="Bottom"
                       PopupAnimation="Fade">
                    <Border Margin="0,8,0,0"
                            Background="White"
                            BorderBrush="{StaticResource PanelBorderBrush}"
                            BorderThickness="1"
                            CornerRadius="12"
                            Padding="8">
                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle" />
                    </Border>
                </Popup>
            </Grid>
            <ControlTemplate.Triggers>
                <DataTrigger Binding="{Binding DataContext.HasSelectedItem, RelativeSource={RelativeSource TemplatedParent}}" Value="True">
                    <Setter TargetName="TopItemBorder" Property="Background" Value="#FF106EBE" />
                </DataTrigger>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter TargetName="TopItemBorder" Property="Background" Value="#FF106EBE" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <ControlTemplate x:Key="SubNavigationMenuItemTemplate" TargetType="MenuItem">
            <Border x:Name="SubItemBorder"
                    Padding="14,10"
                    Background="Transparent"
                    CornerRadius="10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="32" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock VerticalAlignment="Center"
                               FontSize="18"
                               Text="{Binding DataContext.Icon, RelativeSource={RelativeSource TemplatedParent}}" />
                    <StackPanel Grid.Column="1">
                        <TextBlock FontWeight="SemiBold" Text="{TemplateBinding Header}" />
                        <TextBlock Margin="0,2,0,0"
                                   FontSize="12"
                                   Foreground="{StaticResource SubtleTextBrush}"
                                   Text="{Binding DataContext.ViewModel.Description, RelativeSource={RelativeSource TemplatedParent}}" />
                    </StackPanel>
                </Grid>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter TargetName="SubItemBorder" Property="Background" Value="#FFE9F2FB" />
                </Trigger>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter TargetName="SubItemBorder" Property="Background" Value="#FFF2F6FB" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style x:Key="TopNavigationMenuStyle" TargetType="Menu">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style x:Key="SubNavigationMenuItemStyle" TargetType="MenuItem">
            <Setter Property="Header" Value="{Binding Title}" />
            <Setter Property="Command" Value="{Binding DataContext.SelectNavigationItemCommand, RelativeSource={RelativeSource AncestorType=Window}}" />
            <Setter Property="CommandParameter" Value="{Binding}" />
            <Setter Property="IsCheckable" Value="True" />
            <Setter Property="IsChecked" Value="{Binding IsSelected}" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0,0,0,4" />
            <Setter Property="Template" Value="{StaticResource SubNavigationMenuItemTemplate}" />
        </Style>

        <Style x:Key="TopNavigationMenuItemStyle" TargetType="MenuItem">
            <Setter Property="Header" Value="{Binding Title}" />
            <Setter Property="ItemsSource" Value="{Binding Items}" />
            <Setter Property="ItemContainerStyle" Value="{StaticResource SubNavigationMenuItemStyle}" />
            <Setter Property="Template" Value="{StaticResource TopNavigationMenuItemTemplate}" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Foreground" Value="White" />
        </Style>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="18" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="*" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{StaticResource AccentBrush}"
                CornerRadius="16"
                Padding="18,12">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Left" Margin="0,0,24,0" VerticalAlignment="Center">
                    <TextBlock FontSize="22" FontWeight="SemiBold" Foreground="White" Text="Internal Tool Shell" />
                    <TextBlock Margin="0,4,0,0"
                               Foreground="#DDEAF6FF"
                               Text="Web-style navigation for SSO-connected business apps" />
                </StackPanel>

                <Menu DockPanel.Dock="Right"
                      Style="{StaticResource TopNavigationMenuStyle}"
                      ItemContainerStyle="{StaticResource TopNavigationMenuItemStyle}"
                      ItemsSource="{Binding NavigationMenuGroups}" />
            </DockPanel>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="18" />
                <ColumnDefinition Width="280" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Style="{StaticResource CardBorderStyle}"
                    Margin="0">
                <DockPanel>
                    <StackPanel>
                        <TextBlock FontSize="28" FontWeight="SemiBold" Text="{Binding CurrentPage.Title}" />
                        <TextBlock Margin="0,6,0,0"
                                   Foreground="{StaticResource SubtleTextBrush}"
                                   Text="{Binding CurrentPage.Description}" />
                    </StackPanel>
                </DockPanel>
            </Border>

            <Border Grid.Column="2"
                    Style="{StaticResource CardBorderStyle}"
                    Margin="0">
                <StackPanel>
                    <TextBlock FontWeight="SemiBold" Text="Current profile" />
                    <TextBlock Margin="0,8,0,0" Text="{Binding Profile.ProfileName}" />
                    <TextBlock Foreground="{StaticResource SubtleTextBrush}" Text="{Binding Profile.EnvironmentName}" />
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="4"
                Background="Transparent"
                BorderThickness="0">
            <ContentControl Content="{Binding CurrentPage}" />
        </Border>

        <Border Grid.Row="6"
                Style="{StaticResource CardBorderStyle}"
                Margin="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock VerticalAlignment="Center" Text="{Binding StatusMessage}" />
                <TextBlock Grid.Column="1"
                           Foreground="{StaticResource SubtleTextBrush}"
                           Text="Ready for extension: SQL / SharePoint / Graph / Power BI" />
            </Grid>
        </Border>
    </Grid>
</Window>
